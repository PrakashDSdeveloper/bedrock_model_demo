// // responsive.js

// // Function to handle responsiveness
// function handleResponsive() {
//     // Get the width of the viewport
//     var screenWidth = window.innerWidth;

//     // Example: Adjust the font size of a heading based on screen width
//     var heading = document.querySelector('.h4-headers');
//     if (screenWidth < 768) {
//         heading.style.fontSize = '16px'; // Adjust font size for smaller screens
//     } else {
//         heading.style.fontSize = '24px'; // Default font size for larger screens
//     }

//     // Add more responsive adjustments here as needed
// }

// // Call the function initially
// handleResponsive();

// // Listen for window resize events and call the function again
// window.addEventListener('resize', handleResponsive);
